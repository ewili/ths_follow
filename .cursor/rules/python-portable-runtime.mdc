---
description: 便携 Python 部署产物与运行时数据不得入 Git（python311、db、logs、VC 安装包）；pywinauto/THS 交互陷阱
alwaysApply: true
---

# 便携 Python 运行时产物

本仓库为 **signal-server / follow-client** 双端部署，每端含便携 `python311/` 与 Vue `web/`。以下路径**不得提交**（根 `.gitignore` 为准）：

| 路径 | 原因 |
|------|------|
| `**/python311/` | 便携 Python + site-packages（体量极大，本地/部署机重建） |
| `**/*.db` | SQLite 运行时库（含 `data/`、`app/db/`） |
| `**/logs/*` | 按天轮转日志 |
| `**/vc_redist.x64.exe` | VC++ 安装包（~24MB，可本地下载） |
| `**/web/node_modules/`、`**/web/dist/` | 前端依赖与构建产物 |

`data/` 目录可用 `.gitkeep` 占位；`logs/` 由 `settings.py` 自动创建，无需 `.gitkeep`。

## 本地启动（Agent）

- 首次或无 `web/dist/` 时：在对应 `web/` 下 `npm install` + `npm run build`，然后**重启** uvicorn，根路径才会提供 Vue 界面。
- 启动/重启服务前：先执行 Skill **`ths-follow-clear-logs-on-start`** 清空 `**/logs/*`。
- 启动、端口、同花顺 API 连接、联调步骤：遵循项目 Skill **`ths-follow-local-dev`**（不要重复写完整命令序列在本规则内）。

新增 ignore 或清理已跟踪路径时，遵循全局 Skill **`gitignore-untrack-path`**。

## API 路径速查

| 端 | 前缀 | 持仓 | 委托 | 资金 |
|----|------|------|------|------|
| 喊单 | `/api/signal` | `/positions` | `/entrusts` | `/balance` |
| 跟单 | `/api/trader` | `/positions` | `/entrusts` | `/balance` |

注意：跟单端前缀是 **`/api/trader`** 而非 `/api/follow`；持仓和委托路径是**复数**（`/positions`、`/entrusts`）。

## pywinauto / THS 交互陷阱

修改 `easytrader_copy_patch.py` 或 `errors.py` 时，遵循以下约束，同时**双端同步**（见 Skill **`ths-pywinauto-patch-sync`**）：

### OSError [Errno 22] 不是断连

- `OSError: [Errno 22] Invalid argument` 是 pywinauto `process_module()` / `process_get_modules()` 枚举系统受保护进程时的副作用，**不代表同花顺终端断连**。
- `_classify()` 中必须将其映射为 `THS_BUSY`（409，临时性可重试），**绝不能**映射为 `THS_NOT_FOUND`（404，会触发 `with_lock` 清空 trader 导致恶性循环）。

### THS Edit 控件输入方法

- **禁止** `WM_SETTEXT("")` 清空 THS 验证码输入框——会破坏控件内部状态，导致后续 `WM_CHAR` 被吞掉、输入框实际为空。
- **正确做法**：`EM_SETSEL(0, -1)` 全选 + `WM_CHAR` 逐字符覆盖（第一个字符自动替换选中内容）。
- `WM_CLEAR` 也不可靠（部分时序下选中文本未生效）。
- `GetWindowText` 无法读取 THS 自定义 Edit 控件文本，`actual=` 为空不代表输入失败。

### 验证码处理入口约束

- **验证码自动处理仅允许两个入口**：`easytrader_copy_patch.py` 中的 `_copy_get_clipboard_data_patched`（Copy 策略层）和 `_patch_close_pop_dialog`（close_pop_dialog 补丁）。
- **禁止在 `trader_service.py` / `local_trader_service.py` 中内联验证码处理逻辑**——这些服务持有 `asyncio.Lock`，验证码处理可能耗时数分钟（VLM 调用 × 多轮重试），内联会导致全局锁被长时间持有，所有业务请求排队阻塞。
- **`_bring_to_foreground()` 不得触发验证码识别/输入**——它是辅助函数，被多个操作调用，若内部陷入验证码死循环会连锁阻塞全部 GUI 操作。
- **`health_probe` 不触发 `_bring_to_foreground`**——健康检查仅需验证 `_main.exists()`（窗口句柄是否有效），无需窗口置前，更无需处理验证码弹窗。
